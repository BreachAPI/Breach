# Stripped libraries

This folder contains the dependency libraries, stripped and publicized. This for building the project without having to set up the reference paths locally, as well as having build GitHub actions.
