<?xml version="1.0" encoding="utf-8"?>
<configuration>

    <log4net>

        <appender name="ConsoleAppender" type="log4net.Appender.ConsoleAppender">
            <threshold value="INFO" />
            <layout type="log4net.Layout.PatternLayout">
                <conversionPattern value="[%level] [%logger]: %message%newline" />
            </layout>
        </appender>

        <appender name="FileAppender" type="log4net.Appender.RollingFileAppender">
            <file value="Logs/Log.log" />
            <appendToFile value="false" />

            <rollingStyle value="Size" />
            <maxSizeRollBackups value="5" />
            <maximumFileSize value="10MB" />
            <staticLogFileName value="true" />

            <threshold value="DEBUG" />

            <layout type="log4net.Layout.PatternLayout">
                <conversionPattern value="%date [%level] [%logger] - %message%newline" />
            </layout>
        </appender>

        <!-- Used by the web log viewer -->
        <appender name="UdpAppender" type="log4net.Appender.UdpAppender">
            <remoteAddress value="127.0.0.1" />
            <remotePort value="19907" />
            <layout type="log4net.Layout.PatternLayout, log4net">
                <conversionPattern value="[%level] [%logger] - %message%newline" />
            </layout>
        </appender>

        <appender name="DebugAppender" type="log4net.Appender.DebugAppender">
            <threshold value="DEBUG" />

            <filter type="log4net.Filter.LevelMatchFilter">
                <levelToMatch value="DEBUG" />
                <acceptOnMatch value="true" />
            </filter>

            <filter type="log4net.Filter.DenyAllFilter" />

            <layout type="log4net.Layout.PatternLayout">
                <conversionPattern value="[%level] [%logger]: %message%newline" />
            </layout>
        </appender>


        <root>
            <level value="DEBUG" />
            <appender-ref ref="ConsoleAppender" />
            <appender-ref ref="FileAppender" />
            <appender-ref ref="DebugAppender" />
            <appender-ref ref="UdpAppender" />
        </root>

        <!-- The separation of loggers currently doesn't serve any specific purpose. -->
        <!-- It's moreso here for the if it's needed in the future. -->
        <!-- But it can be used to omit specific logs e.g. vanilla during mod development. -->

        <logger name="VANILLA">
            <level value="DEBUG" />
        </logger>

        <logger name="BREACH">
            <level value="DEBUG" />
        </logger>

        <logger name="DEBUG">
            <level value="DEBUG" />
        </logger>

        <logger name="MOD">
            <level value="DEBUG" />
        </logger>

    </log4net>

</configuration>